/**
 * Hook for managing polling interval settings
 * Re-exports from context for backward compatibility
 */

export { usePollingSettings, POLLING_INTERVALS } from '@/contexts/PollingSettingsContext';
export type { PollingInterval } from '@/contexts/PollingSettingsContext';
